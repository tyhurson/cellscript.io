{
  "name": "@cellscript/web-server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Tyler Hurson",
  "license": "MIT",
  "version": "0.0.1",
  "scripts": {
    "build": "yarn run collect-public && yarn webpack",
    "build:watch": "yarn run collect-public && yarn webpack --watch",
    "clean": "scripty",
    "collect-public": "scripts/collect-public.sh",
    "compile": "scripty",
    "lint": "scripty",
    "start": "node dist/index.js",
    "start:watch": "yarn run build && yarn concurrently \"yarn run build:watch\" \"yarn nodemon dist/index.js --watch dist\"",
    "test": "echo temporarily disabled"
  },
  "scripty": {
    "path": "../../scripts"
  },
  "dependencies": {
    "@cellscript/util": "^0.0.1",
    "@types/express": "^4.17.12",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.7"
  },
  "files": [
    ".ebextensions",
    "dist"
  ]
}
